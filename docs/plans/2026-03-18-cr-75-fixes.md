# Code Review 75+ Fixes

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix 2 issues scoring 75+ from the PR #8 code review.

**Architecture:** Two independent fixes: (1) replace MagicMock of AuditLogger with real SQLite-backed instance in test_loop_emergency.py, (2) pin CI Python version to 3.12.

**Tech Stack:** Python 3.12, pytest, GitHub Actions

---

### Task 1: Replace AuditLogger mock with real instance in test_loop_emergency.py

**Issue:** CLAUDE.md says "Claude Code is mocked at the ClaudeCodeBridge boundary — the only mock in the suite." The test mocks AuditLogger and EnvironmentFacade with MagicMock.

**Files:**
- Modify: `tests/unit/core/test_loop_emergency.py`

**Fix:** Replace `audit = MagicMock()` / `audit.log = AsyncMock()` with a real `AuditLogger(db=db.db)` initialized against the test's SQLite database. The `env` (EnvironmentFacade) mock is acceptable since it wraps ClaudeCodeBridge (the allowed mock boundary).

---

### Task 2: Pin CI Python version to 3.12

**Issue:** `.github/workflows/ci.yml` uses `python-version: '3.x'` which resolves to latest (3.13), not the project's declared minimum of 3.12.

**Files:**
- Modify: `.github/workflows/ci.yml`

**Fix:** Change `python-version: '3.x'` to `python-version: '3.12'`.
